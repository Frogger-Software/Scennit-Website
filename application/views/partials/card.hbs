<div id="post-{{id}}" class="card flex-container grid-item">
    <img class="card-image" src="{{thumbnail}}" alt="Missing Image">
    <div class="card-body">
        <p class="hide-overflow small-title">{{title}}</p>
        <p class="card-text hide-overflow">{{description}}</p>
        <a href="/post/{{id}}" class="anchor-buttons">Post Details</a>
    </div>
</div>